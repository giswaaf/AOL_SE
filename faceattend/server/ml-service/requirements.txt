# ============================================================
# requirements.txt — ML Service (Gabungan 3 Repository)
# ============================================================
# Letakkan di: server/ml-service/requirements.txt
#
# URUTAN INSTALASI PENTING:
# 1. Install cmake & dlib prerequisites dulu (lihat README)
# 2. pip install -r requirements.txt
# ============================================================

# ── Dari ageitgey/face_recognition ──────────────────────────
face_recognition>=1.3.0
dlib>=19.22.0

# ── Dari minivision-ai/Silent-Face-Anti-Spoofing ────────────
torch>=1.7.0
torchvision>=0.8.0

# ── Dari nem-web/smart-attendance (ML Service) ──────────────
fastapi>=0.115.5
uvicorn[standard]>=0.32.1
httpx>=0.27.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# ── Shared dependencies ─────────────────────────────────────
opencv-python-headless>=4.5.0
numpy>=1.21.0,<2.0.0
Pillow>=8.0.0
scikit-learn>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── Opsional (untuk GPU acceleration) ───────────────────────
# torch-cu117  # Uncomment jika ada NVIDIA GPU + CUDA 11.7

# ── Face Embedding Models ───────────────────────────────────
insightface>=0.7.3
onnxruntime>=1.16.0
deepface>=0.0.79
